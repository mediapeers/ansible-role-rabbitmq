---
galaxy_info:
  author: michaeljs1990
  description: EC2 rabbitmq auto clustering
  company: nowait
  license: license (GPLv2, CC-BY, etc)
  min_ansible_version: 2.4
  platforms:
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  categories:
    - cloud
    - cloud:ec2
    - clustering
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
